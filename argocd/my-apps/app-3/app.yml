apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx-app-3
  labels:
    app: nginx
spec:
  replicas: 3
  selector:
    matchLabels:
      app: nginx
  template:
    metadata:
      labels:
        app: nginx-app-3
    spec:
      containers:
      - name: nginx-app-3
        image: nginx:latest
        ports:
        - containerPort: 80